int calculateCarExpenses(struct trip trip)
{
    int costs;

    // ask for vehicle type (car or taxi)

    // if car, ask for mileage
    // costs = mileage * 0.27 (calculate mileage compensation)
    // ask for parking expenses
    //// (let user know that <= $6 will be paid back)
    //// costs += parking

    // if taxi, ask for taxi fee
    //// (let user know that <=$10 will be paid back)

    // costs = taxi fee
    // return costs

    return costs;
}